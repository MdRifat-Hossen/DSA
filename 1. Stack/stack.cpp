#include <bits/stdc++.h>
using namespace std;
int main()
{
    /* //   creation stack
     stack<int> s;

     // Push oparetion

     s.push(1);
     s.push(2);
     // s.push(3);
     // s.push(4);

     // Pop operation

     s.pop();

     // Top elemet WHich is this
     cout << " TOp element is this " << s.top() << endl;

     // Stack is empty
     if (s.empty())
     {
         cout << "Stack is empty" << endl;
     }
     else
         cout << "stack is not empty" << endl;

     cout << "Stack of size is" << s.size();
     */

    return 0;
}